{
  "name": "@getbooked/xestro",
  "version": "0.1.0",
  "private": true,
  "description": "PMS integration layer: canonical FHIR-shaped model, PmsConnector interface (Xestro target contract), mock connector, slot-hold and patient-match primitives",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^4.1.0"
  }
}
